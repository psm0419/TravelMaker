<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user_mapper">

	  <insert id="insertUser" parameterType="User">
        INSERT INTO users (user_id, username, nickname, pw, email, tel, jumin, user_type)
        VALUES (#{userId}, #{username}, #{nickname}, #{pw}, #{email}, #{tel}, #{jumin}, #{userType})
    </insert>

    <select id="getUserById" resultType="User">
        SELECT * FROM users WHERE user_id = #{userId}
    </select>

    <update id="updateUser" parameterType="User">
        UPDATE users 
        SET username= #{username}, nickname=#{nickname}, pw=#{pw}, 
            email= #{email}, tel= #{tel}, jumin=#{jumin}, user_type=#{userType}
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteUser" parameterType="User">
        DELETE FROM users WHERE user_id = #{userId}
    </delete>

    <select id="getAllUsers" resultType="User">
        SELECT * FROM users
    </select>


</mapper>